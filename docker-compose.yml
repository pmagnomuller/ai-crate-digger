services:
  mongodb:
    image: mongo:7
    container_name: ai-crate-digger-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ai-crate-digger
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
